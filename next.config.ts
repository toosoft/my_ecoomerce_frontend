import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
    images: {
    domains: ["static.vecteezy.com", "images.unsplash.com", "t4.ftcdn.net"],
  },
};

export default nextConfig;
